[build]
  publish = "dist"
  command = "npm run build"

# Redirection SPA pour toutes les routes (les fichiers statiques sont servis automatiquement)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
